void autoexec() {
    int x = 1;
    
    y = ++x;
    
    if ( !x ) log("blart");
    
    for (i = 0; i < 10; i++) {
        log("blurt");
    }
    
    while ( x < 10 ) {
        x++;
    }

    x = add(5,6);
}

int add(int x, int y) {
    return x + y;
}
