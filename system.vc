void autoexec() {
    log("blart" + (4+1+2) + "foo");
    
    int a = 5;
    int b = 7;
    int c = 2;
    
    log(a + b + c);
    
    if ( a < b < c) {
        log("asc");
    } else if ( a > b > c) {
        log("desc");
    } else if ( a == b == c ) {
        log("eq");
    } else {
        log("not sorted");
    }
}
